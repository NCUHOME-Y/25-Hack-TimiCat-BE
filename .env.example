# 运行环境
ENV=dev
ADDR=:3001

# JWT（开发占位）
JWT_SECRET=dev-guest-secret

# PostgreSQL（配合 docker-compose 使用）
PGUSER=app
PGPASSWORD=app
PGDATABASE=appdb
PGHOST=localhost
PGPORT=5432

# 允许的前端域名（CORS）
ALLOW_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173
