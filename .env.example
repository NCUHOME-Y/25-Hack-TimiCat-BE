# App
ENV=dev
ADDR=:3001
MIN_SESSION_SEC=60

# DB
PGUSER=app
PGPASSWORD=app
PGDATABASE=appdb
PGPORT=5432
DB_DSN=postgres://app:app@localhost:5432/appdb?sslmode=disable

# JWT/Secret（游客登录不强依赖）
JWT_SECRET=replace-with-a-strong-secret
